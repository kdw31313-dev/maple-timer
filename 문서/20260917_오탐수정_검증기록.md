# 2026-09-17 룬·원형 거탐 오탐 수정 기록

## 상태

로컬 수정본. 원격 저장소 push, PR 생성·병합, 실사이트 배포는 하지 않았다.
실제 양성 누락 방지를 우선하며, 원형 클릭형 양성 자료를 추가 확인하기 전에는 배포하지 않는다.
기준 버전: `584f5c6c5a9fbb21adcaf84a61704826d9a338ac` (PR #50).

## 이번 자료와 판단

사용자가 제공한 2026-09-16 사진 5장 중 **3번은 사용자가 정상 룬 검출로 확인**했다.

| 사진 | 수정 전 재현 | 수정 후 확인 | 해석 제한 |
|---|---|---|---|
| 1 원형 오탐 | 첨부 JPEG에서 원형 후보 자체는 재현되지 않음. 표시 수치 2.02 / 67%는 기존 경계 조건 통과 | 해당 수치 조합을 차단. 이동 원판 합성 양성은 3프레임에 알림 | 당시 원본 연속 프레임과 실제 원형 양성 검증 필요 |
| 2 룬 오탐 | 1.5배 재현에서 공유 시작·배경 학습 완료 상태 모두 1회 오탐 | 1·1.5·2배, 시작·학습 완료 경로 모두 0회 | 표시가 추가된 JPEG와 합성 시간 입력을 사용 |
| 3 정상 룬 | 1.5배, 빈 보라 배경 학습 완료 상태에서 1회 알림 | 동일 상태에서 600ms에 1회 유지 | 원래 JPEG 크기에서는 전후 모두 후보 없음. 1.5배는 원본 1080p의 대체 원본이 아님 |
| 4 룬 오탐 | 1.5배 학습 완료 상태에서 별도 보라 장식 후보로 1회 오탐 | 같은 입력에서 0회. 다른 시험 크기에서도 0회 | 재현 후보가 사진의 노란 상자 위치와 다르므로 원래 사건 해결 확정 불가 |
| 5 룬 오탐 | 시험 크기 모두 오탐 재현 안 됨 | 계속 0회 | 해결됐다는 근거가 아님. 전후 영상 필요 |

## 변경 내용

- `js/룬검출정확도.js`: 가로로 퍼지고, 진한 분홍 seed가 전체 보라 영역의 34% 미만이며, 중심-외곽의 적색·청색 평균 밝기 차이가 45를 넘는 후보를 배경 발광 장식으로 구분한다. 세 조건을 동시에 만족해야 차단하며 시작·학습 완료 경로에 함께 적용한다. 색상 또는 좌표만으로 차단하지 않는다.
- `js/거탐검출정확도.js`: 방향 일치가 70% 미만인 약한 원형 경계에 한해 정규화 반지름 편차 상한을 2.05에서 1.90으로 좁힌다. 방향 일치가 70% 이상인 이동 원형의 2.45 상한과 기존 색 검증은 유지한다.
- 떠다니는 글자형, 클릭 준비형, 알림 2회·3초 간격, 사건당 텔레그램 사진 1회 정책은 변경하지 않았다.
- `index.html`: 향후 배포 시 변경 JS를 새로 받도록 두 파일의 버전 문자열을 `20260917-1`로 준비했다. 이 변경만으로 실사이트가 갱신되지는 않는다.

## 자동 검사

- 기존 6종 통과: 룬영상보완회귀, 발동안내거탐회귀, 클릭준비거탐회귀, 저부하감지회귀, 거탐빠른검사회귀, 백그라운드감시회귀.
- 추가 사진/합성 검사: `테스트/구월오탐회귀.js <5장 사진 폴더>` 통과. 정상 룬의 600ms는 합성 반복 입력 확인 시간이며 실사용 지연의 보장이 아니다.
- `node --check`, `git diff --check` 통과.
- 기존 오탐 사진 23장: 각 1.2초 반복 재생, 수정 후 거탐 알림 0회. 사진 반복은 원래 전투의 시간 변화를 재현하지 않으므로 동영상 음성 검증과 구분한다. 결과: `../검증자료_20260916/기존오탐23장_수정후.json`.
- 사진 재생기를 기존 0.45초에서 1.2초로 늘렸다. 0.45초는 원형형의 300ms 간격 정밀 3프레임 판정에 도달하지 못할 수 있어 음성 검사에 불충분했다. 동영상 재생 간격은 변경하지 않았다.
- 실제 텔레그램 전송과 PC 소리는 발생시키지 않았다. 알림 호출을 기록하는 모형으로 검사했다.

## 원본 영상 비교

Google Drive의 `거짓말 탐지기 자료/2026-08-21_발동안내_개선/01_원본 영상`에 있는 8개 원본을 별도 로컬 검증 폴더에 받았다. 총 463,732,450바이트. 파일별 크기와 SHA-256은 `../검증자료_20260916/원본영상_무결성.json`에 있다.

전체 길이는 450초, 버전당 3,000프레임(초당 20/3 프레임)이다. 기존 재생기와 같은 240×135 AREA 축소·빠른 검사 모형을 사용한다. 브라우저 Canvas의 실제 리샘플링·스케줄 지연과 동일하지는 않다.

수정 전후 전체 비교: **8/8 완료**. 양쪽 모두 원본 전체 450초·3,000프레임이며, 알림 유형·시각·횟수와 발동 안내 후보 수가 동일하다. 비교 스크립트의 일치 검사 통과. 자세한 결과는 `../검증자료_20260916/원본8개_전후비교.json`에 있다.

| 원본 | 최초 알림(영상 시작 기준, 초) | 전후 알림 수 |
|---|---:|---:|
| 2026-08-20_거탐_01 | 2.25 | 2 → 2 |
| 2026-08-20_거탐_02 | 24.00 | 2 → 2 |
| 2026-08-21_거탐_원본 | 5.10 | 2 → 2 |
| 2026-08-21_거탐_추가_02 | 3.30 | 2 → 2 |
| 2026-08-24_거탐_01 | 3.90 | 2 → 2 |
| 2026-08-24_거탐_02 | 1.95 | 4 → 4 |
| 2026-08-25_거탐_01 | 0.60 | 2 → 2 |
| 2026-08-25_거탐_02 | 0.90 | 2 → 2 |

8월 21일 추가 영상의 단일 원형 후보 1프레임은 이번 경계 조건으로 제거됐다. 해당 후보가 실제 원형인지 시점별 정답 검증은 하지 않았으므로 오탐 제거 실적으로 세지 않는다. 8월 25일 두 번째 영상의 원형 후보 2프레임은 유지됐다. 어느 쪽도 원형형의 연속 양성 검증을 대신하지 않는다.

```powershell
node 테스트/영상결과비교.js ../검증자료_20260916/수정전_7영상.json ../검증자료_20260916/수정후_7영상.json ../검증자료_20260916/수정전_추가1영상.json ../검증자료_20260916/수정후_추가1영상.json
```

기존 8개 영상에서 알림이 한 번 이상 발생했다는 것만으로, 모든 유형·모든 등장 시점이 적시에 검출됐다고 할 수 없다. 특히 8월 20일 두 번째 영상의 최초 알림은 영상 시작 24초 지점이며 실제 등장 시점부터의 지연과 구분해야 한다. 원형 후보가 일부 프레임에서 산출되더라도 실제 원형 양성이라는 정답 판정이 없다면 양성 검증으로 세지 않는다.

## 다음 작업 / 배포 보류 이유

1. 기존 원형 클릭형 양성 사진 2장(과거 첨부 `272fb1f2`의 3·4번) 또는 실제 원형 진행 영상 확보 후 전후 비교.
2. 가능하면 이번 1·5번 오탐 직전·직후 원본 영상 확보. 4번도 원래 노란 상자 후보의 시간 흐름을 추가 확인.
3. 양성 보존과 신규 오탐 개선 확인 뒤 PR·배포 진행. 긴 알림 잠금으로 오탐을 숨기거나 거탐 전체 감도를 낮추지 않는다.

이번 환경의 지정 작업 폴더에는 첨부 사진만 있었으므로 저장소를 새로 복제했다. 후속 확인에서 기존 폴더가 `G:/내 드라이브/00. 김동우/05. AI/00. 메이플스토리`라는 이름으로 존재함을 확인했다. 그곳의 기존 작업본은 깨끗한 main 상태이며 건드리지 않았다. 보유 자료 목록에도 과거 원형 양성 2장은 찾지 못했다.

원본 사진과 Drive 파일은 수정·이동·삭제하지 않았다. 현재 지정 작업 폴더 안의 비교용 `기준코드_20260916` 작업 트리와 `검증자료_20260916`은 재현을 위해 보존했다.
