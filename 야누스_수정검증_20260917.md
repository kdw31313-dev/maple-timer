# 야누스 소멸 알림 수정 검증 (2026-09-17)

## 배포 상태

로컬 수정·검증 완료 후 사용자가 공개 저장소를 통한 사이트 배포를 명시적으로 승인했다. 배포 브랜치는 `fix/janus-icon-reliability-20260917`, 야누스 자산 버전은 `20260917-2`이다. 이 문서는 해당 변경의 검증 기록이며 운영 반영 여부는 연결된 PR 병합 및 GitHub Pages 배포 상태로 확인한다. 영상 원본·개인 설정·인증정보는 배포 대상에서 제외했다.

## 확인한 문제

- 네 번째 영상에서 기존 판별기가 실제 소멸 후 다른 버프를 계속 야누스로 추적하여 알림이 누락되는 현상을 재현했다.
- 세 번째 영상은 기존 코드의 순차 재생에서는 알림이 나왔다. 당시 실제 화면 공유 상태/콘솔 기록이 없어 실사용 누락의 단일 원인은 확정하지 않았다.
- 코드상 분석 간격이 2.5초를 넘거나 일시적으로 화면 프레임을 얻지 못하면 활성 추적을 지우는 위험이 있었다.

## 수정

- 네 제공 영상에서 육안 검수한 28개 아이콘 표본과 소멸 이후 다른 버프 208개를 대조한다. 신경망 재학습이 아닌 경량 표본 비교 방식이다.
- 숫자와 바깥 배경 대신 아이콘 내부 곡선을 비교한다. 숫자가 없는 회색 종료 위상도 아이콘 존재로 취급한다.
- 최초 아이콘 확인 후 감시하고, 유효한 새 화면에서 1초 부재를 확인하면 소리와 텔레그램 사진 요청을 각 1회 발생시킨다. 새 아이콘을 확인하면 다시 감시한다.
- 재사용으로 시간이 늘어나도 만료 시각을 추정하지 않는다. 고정 100초 타이머는 없다.
- 일시적인 화면 공백/분석 지연은 추적 상태를 보존하고 부재 확인 시간만 다시 시작한다. 명시적인 공유 종료/설정 해제/화면 크기 변경은 초기화한다.
- 기존 위치와 첫 버프 줄을 우선 확인하며 Worker에서 분석한다. 분석 요청을 겹치지 않고, 대기 중 600ms / 추적 중 150ms 기회로 제한한다. 작업 불가능 시에는 화면 복사도 생략한다.
- 룬/거탐 핵심 판별 코드와 검사 주기는 변경하지 않았다.

## 검증 범위와 한계

- 네 원본은 HEVC이므로 실제 브라우저 재생 검증에는 원본 보존 H264 사본을 사용했다. 순차 판정 검증에는 원본을 직접 디코딩했다.
- 네 영상은 보정과 회귀 확인에 함께 사용했으므로 독립된 미지 영상 정확도나 모든 환경에서의 무오류를 뜻하지 않는다.
- 알림 시간 기준은 부재 확인 1초이며 실제 화면 샘플링, 분석, 브라우저 부하 및 네트워크 때문에 전달은 더 늦을 수 있다.
- 브라우저 검증은 실제 소리 재생 함수와 AudioContext 상태, 텔레그램 사진 요청까지 확인하되 외부 전송은 모의 응답으로 처리한다. 사용자 휴대폰 실수신 및 실제 게임 FPS는 별도 확인 대상이다.

## 통과한 자동 회귀

- 야누스 상태/종료 위상/재사용/화면 공백/1000ms 경계
- Worker 로드/중복 요청 차단/오래된 결과 폐기
- 룬 및 거탐 주기 보존, 같은 영상 프레임 중복 분석 방지
- 거탐 빠른 검사, 발동 안내형, 클릭 준비형, 룬 영상 보완, 백그라운드 감시

## 실시간 브라우저 재생 결과

| 영상 | 소멸 알림 시점(영상 시간) | 소리 | 사진 전송 요청 | 실행 오류 |
|---|---:|---:|---:|---:|
| 1 | 40.727초 | 1 | 1 | 0 |
| 2 | 88.047초 | 1 | 1 | 0 |
| 3 | 67.298초 | 1 | 1 | 0 |
| 4 | 162.601초 | 1 | 1 | 0 |

네 영상 모두 전체 길이를 실제 속도로 재생했다. 두 번째 영상 끝의 재사용은 새 활성 상태로 유지되고 추가 소멸 알림을 내지 않았다. 네 번째 영상 중간의 재사용도 소멸로 처리하지 않았다.

Worker 분석 중앙값은 0.8~0.9ms였다. 전체 탐색/부하가 있는 프레임은 더 오래 걸렸으며 네 번째 재생에서는 최대 약 1199ms의 분석 지연과 메인 스레드 긴 작업도 관측됐다. 따라서 평균만으로 렉이 없다고 판단하지 않고 별도의 같은 구간 켜짐/꺼짐 대조를 수행한다.

### 부하 대조

네 번째 영상의 100초부터 같은 구간을 30초씩, 룬/거탐을 모두 켠 상태로 비교했다. 야누스 OFF에서도 100~800ms 수준의 긴 작업이 반복 관측됐다. ON의 야누스 Worker 분석은 중앙값 1ms, p95 6.2ms, 최대 140.8ms였다. ON에도 긴 작업(초기 최대 3594ms 포함)이 있었으므로 야누스 추가 여부만으로 모든 지연을 설명하거나 실게임 무렉을 보장할 수 없다. 이 검증은 화면 공유 스트림이 아니라 영상 파일을 재생하는 headless Chromium 환경이다. 실제 게임 FPS와 사용 중 브라우저의 장기 부하는 미측정이다.

원본 네 개는 거탐 자료와 분리한 비공개 Google Drive `야누스 영상 자료` 폴더에 복사 업로드했고, 파일 목록·크기를 다시 확인했다. 원본은 이동하거나 삭제하지 않았다.
